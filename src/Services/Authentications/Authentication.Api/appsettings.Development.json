{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "ConnectionStrings": {
    "AuthenticationDatabase": "Host=localhost;Port=5432;Database=storyverse_authentication;Username=postgres;Password=postgres"
  },
  "ServiceAuth": {
    "Token": "local-dev-service-token"
  },
  "GcpSettings": {
    "AuthSettings": {
      "// SecretKey": "Local-development-only placeholder. Do not reuse in any shared or production environment.",
      "SecretKey": "local-development-only-placeholder-key-not-for-production-use-0123456789"
    }
  },
  "// Seed": "Dev only. These emails are promoted to PlatformAdmin on startup once they register via POST /v1/auth/register.",
  "Seed": {
    "PlatformAdminEmails": [ "admin@storyverse.local" ]
  }
}
