{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "CommunityDatabase": ""
  },
  "// ServiceAuth": "Shared secret for service-to-service calls to Community's internal comment moderation endpoint. Empty on purpose: provide via env ServiceAuth__Token outside local dev.",
  "ServiceAuth": {
    "Token": ""
  },
  "// Services": "Downstream service endpoints. AuthApi backs the comment/rating author display-name lookup. Provide via env (Services__AuthApi__BaseUrl / __ServiceToken) outside local dev.",
  "Services": {
    "AuthApi": {
      "BaseUrl": "",
      "ServiceToken": ""
    }
  },
  "GcpSettings": {
    "AuthSettings": {
      "Issuer": "Authentication.Api",
      "Audiences": [ "StoryVerse.Web", "StoryVerse.Mobile" ],
      "// SecretKey": "Placeholder only. Never commit a real signing key here. Provide it via environment variable (e.g. GcpSettings__AuthSettings__SecretKey) or GCP Secret Manager, per auth-guidelines.md section 12.",
      "SecretKey": "REPLACE_VIA_ENVIRONMENT_VARIABLE_OR_SECRET_MANAGER"
    }
  }
}
