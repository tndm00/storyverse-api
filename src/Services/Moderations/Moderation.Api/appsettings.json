{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "ModerationDatabase": ""
  },
  "// Services": "Downstream services the Moderation service calls. ContentApi/CommunityApi apply Hide/Remove decisions and resolve target titles; AuthApi resolves reporter names. *.ServiceToken must match that service's ServiceAuth:Token. Provide via env (Services__ContentApi__BaseUrl / __ServiceToken, ...) outside local dev; empty disables the call.",
  "Services": {
    "ContentApi": {
      "BaseUrl": "",
      "ServiceToken": ""
    },
    "CommunityApi": {
      "BaseUrl": "",
      "ServiceToken": ""
    },
    "AuthApi": {
      "BaseUrl": "",
      "ServiceToken": ""
    }
  },
  "GcpSettings": {
    "AuthSettings": {
      "Issuer": "Authentication.Api",
      "Audiences": [ "StoryVerse.Web", "StoryVerse.Mobile" ],
      "// SecretKey": "Placeholder only. Never commit a real signing key here. Provide it via environment variable (e.g. GcpSettings__AuthSettings__SecretKey) or GCP Secret Manager, per auth-guidelines.md section 12.",
      "SecretKey": "REPLACE_VIA_ENVIRONMENT_VARIABLE_OR_SECRET_MANAGER"
    }
  }
}
